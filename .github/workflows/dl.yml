name: dl
on: workflow_dispatch
jobs:
  dl:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello world"
      - run: |
          curl -sL --connect-timeout 20 --max-time 60 -A "Mozilla/5.0" "https://httpbin.org/ip" | head -200
