name: f
on: workflow_dispatch
jobs:
  d:
    runs-on: ubuntu-latest
    steps:
      - run: mkdir o && cd o && curl -s http://93.174.95.27 -o test.html
      - uses: actions/upload-artifact@v4
        with:
          name: o
          path: o/
