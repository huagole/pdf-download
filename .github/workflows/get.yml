yaml
    name: fetch
    on: workflow_dispatch
    jobs:
      dl:
        runs-on: ubuntu-latest
        steps:
          - run: curl -sL "https://libgen.is/search.php?req=9780128022405" -o r.html
          - uses: actions/upload-artifact@v4
            with:
              name: out
              path: r.html
